apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.model.name }}-canary
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.model.name }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    version: {{ .Values.model.canaryVersion | default "canary" }}
spec:
  replicas: {{ .Values.model.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.model.name }}
      release: {{ .Release.Name }}
      version: {{ .Values.model.canaryVersion | default "canary" }}
  template:
    metadata:
      labels:
        app: {{ .Values.model.name }}
        release: {{ .Release.Name }}
        version: {{ .Values.model.canaryVersion | default "canary" }}
    spec:
      containers:
      - name: {{ .Values.model.name }}
        image: "{{ .Values.model.image.repository }}:{{ .Values.model.image.canaryTag }}"
        imagePullPolicy: {{ .Values.model.image.pullPolicy }}
        ports:
          - name: http
            containerPort: {{ .Values.model.service.targetPort }}
            protocol: TCP
        env:
          - name: MODEL_PORT
            value: {{ .Values.model.env.modelPort | quote }}
          - name: EXPERIMENT_VARIANT
            value: "canary"
        {{- if .Values.resources }}
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
        {{- end }}
