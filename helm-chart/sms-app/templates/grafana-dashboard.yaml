apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-custom
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  sms-app-dashboard.json: |
{{ .Files.Get "dashboards/metric_dashboard.json" | indent 4 }}
  canary-analysis.json: |
{{ .Files.Get "dashboards/continuous_experimentation_dashboard.json" | indent 4 }}